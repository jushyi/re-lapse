---
phase: 29-documentation
plan: 03
type: execute
---

<objective>
Add JSDoc documentation to all service files and remaining hooks.

Purpose: Enable developers to understand service APIs without reading implementation details.
Output: JSDoc comments on all exported functions in services and remaining hooks.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-documentation/29-CONTEXT.md
@src/services/firebase/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to Firebase services</name>
  <files>src/services/firebase/darkroomService.js, src/services/firebase/feedService.js, src/services/firebase/friendshipService.js, src/services/firebase/phoneAuthService.js, src/services/firebase/photoService.js, src/services/firebase/storageService.js, src/services/firebase/userService.js, src/services/firebase/notificationService.js, src/services/firebase/signedUrlService.js, src/services/firebase/accountService.js</files>
  <action>
Add JSDoc to all exported functions in Firebase service files. For each file:

1. Add file header comment (if missing) explaining the service's purpose
2. Add JSDoc to each exported function with:
   - Brief one-line description
   - @param {type} name - description for each parameter
   - @returns {Promise<{success: boolean, error?: string, ...}>} description

Example format:

```javascript
/**
 * Fetches all developing photos for a user
 * @param {string} userId - The user's Firebase UID
 * @returns {Promise<{success: boolean, photos?: Array, error?: string}>} Result object
 */
```

Services to document (10 files):

- darkroomService.js - Batch reveal system functions
- feedService.js - Feed queries and reaction handling
- friendshipService.js - Friend request and relationship functions
- phoneAuthService.js - Phone authentication flow
- photoService.js - Photo CRUD and lifecycle functions
- storageService.js - Firebase Storage upload/download
- userService.js - User profile functions
- notificationService.js - Push notification handling
- signedUrlService.js - Signed URL generation
- accountService.js - Account deletion

Keep JSDoc minimal. Do NOT add @example blocks or edge case documentation. Do NOT add emojis.
</action>
<verify>Each Firebase service file has JSDoc on all exported functions</verify>
<done>All 10 Firebase service files have consistent JSDoc documentation</done>
</task>

<task type="auto">
  <name>Task 2: Add JSDoc to utility services and remaining hooks</name>
  <files>src/services/uploadQueueService.js, src/services/secureStorageService.js, src/hooks/useCamera.js, src/hooks/useFeedPhotos.js</files>
  <action>
Add JSDoc to remaining service and hook files:

For uploadQueueService.js:

- Add file header explaining the background upload queue system
- Add JSDoc to exported functions (enqueue, processQueue, etc.)

For secureStorageService.js:

- Add file header explaining secure storage for sensitive data
- Add JSDoc to exported functions (save, get, delete, etc.)

For useCamera.js:

- Add file header explaining camera screen state management
- Add JSDoc to the main useCamera hook with all params
- Add JSDoc to key callbacks (handleCapture, handleFlipCamera, etc.)

For useFeedPhotos.js:

- Add file header explaining feed data management
- Add JSDoc to the main useFeedPhotos hook
- Add JSDoc to returned functions (refresh, loadMore, etc.)

Same JSDoc format as Task 1. Keep minimal. Do NOT add emojis.
</action>
<verify>All 4 files have JSDoc on exported functions</verify>
<done>All utility services and remaining hooks have consistent JSDoc documentation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 10 Firebase service files have JSDoc
- [ ] uploadQueueService.js has JSDoc
- [ ] secureStorageService.js has JSDoc
- [ ] useCamera.js has JSDoc
- [ ] useFeedPhotos.js has JSDoc
- [ ] No emojis in any documentation
- [ ] npm run lint passes (if configured)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- All service APIs are documented
- All hooks have consistent documentation
- Phase 29 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/29-documentation/29-03-SUMMARY.md`:

Include:

- List of all files documented
- Total functions documented
- Note that Phase 29 (Documentation) is complete
- Note that v1.6 milestone is complete
  </output>
