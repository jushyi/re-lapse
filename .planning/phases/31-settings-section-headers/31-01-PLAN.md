---
phase: 31-settings-section-headers
plan: 01
type: execute
---

<objective>
Add section headers to SettingsScreen to visually organize menu items into logical categories.

Purpose: Improve Settings page scannability and match standard iOS/Android settings patterns.
Output: SettingsScreen with section headers grouping items into Account, Privacy, Legal, and Support categories.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-settings-section-headers/31-CONTEXT.md

# Existing section header pattern (from FriendsScreen):

@src/styles/FriendsScreen.styles.js (lines 100-112)

# Target file:

@src/screens/SettingsScreen.js

**Prior decisions:**

- Phase 16: All colors use constants from colors.js
- Phase 29: Settings has Help & Support link and version display

**Existing pattern from FriendsScreen.styles.js:**

```javascript
sectionHeader: {
  backgroundColor: colors.background.primary,
  paddingHorizontal: 16,
  paddingVertical: 10,
  borderBottomWidth: 1,
  borderBottomColor: colors.border.subtle,
},
sectionHeaderText: {
  fontSize: 14,
  fontWeight: '600',
  color: colors.text.secondary,
  textTransform: 'uppercase',
  letterSpacing: 0.5,
}
```

**Section organization (from 31-CONTEXT.md):**

- **Account:** Edit Profile
- **Privacy:** Blocked Users, Recently Deleted
- **Legal:** Privacy Policy, Terms of Service
- **Support:** Help & Support
- Sign Out and Delete Account remain at bottom (no header - they're clearly action buttons)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure menu items into sections</name>
  <files>src/screens/SettingsScreen.js</files>
  <action>
Convert the flat `menuItems` array into a `sections` array where each section has a `title` and `items` array.

Section structure:

```javascript
const sections = [
  {
    title: 'Account',
    items: [
      { id: 'editProfile', label: 'Edit Profile', icon: 'person-outline', onPress: ... },
    ],
  },
  {
    title: 'Privacy',
    items: [
      { id: 'blockedUsers', label: 'Blocked Users', icon: 'ban-outline', onPress: ... },
      { id: 'recentlyDeleted', label: 'Recently Deleted', icon: 'trash-outline', onPress: ... },
    ],
  },
  {
    title: 'Legal',
    items: [
      { id: 'privacy', label: 'Privacy Policy', icon: 'document-text-outline', onPress: ... },
      { id: 'terms', label: 'Terms of Service', icon: 'document-outline', onPress: ... },
    ],
  },
  {
    title: 'Support',
    items: [
      { id: 'helpSupport', label: 'Help & Support', icon: 'help-circle-outline', onPress: ... },
    ],
  },
];
```

Keep Sign Out and Delete Account as separate `actionItems` array (no header):

```javascript
const actionItems = [
  { id: 'signout', label: 'Sign Out', icon: 'log-out-outline', onPress: handleSignOut },
  {
    id: 'delete',
    label: 'Delete Account',
    icon: 'trash-outline',
    onPress: handleDeleteAccount,
    danger: true,
  },
];
```

Add section header styles following FriendsScreen pattern:

- sectionHeader: paddingHorizontal 16, paddingVertical 10, borderBottom subtle
- sectionHeaderText: fontSize 14, fontWeight 600, uppercase, letterSpacing 0.5, text.secondary color
  </action>
  <verify>No syntax errors when saving file</verify>
  <done>menuItems converted to sections array + actionItems array, styles added</done>
  </task>

<task type="auto">
  <name>Task 2: Update render to display section headers</name>
  <files>src/screens/SettingsScreen.js</files>
  <action>
Replace the single menuItems.map() with:
1. sections.map() that renders section header + items for each section
2. actionItems.map() for Sign Out and Delete Account (no header)

Render pattern:

```jsx
{/* Sectioned Menu Items */}
<View style={styles.menuContainer}>
  {sections.map((section) => (
    <View key={section.title}>
      <View style={styles.sectionHeader}>
        <Text style={styles.sectionHeaderText}>{section.title}</Text>
      </View>
      {section.items.map((item) => (
        <TouchableOpacity key={item.id} style={styles.menuItem} ... >
          {/* existing item render */}
        </TouchableOpacity>
      ))}
    </View>
  ))}

  {/* Action Items (Sign Out, Delete Account) - no header */}
  {actionItems.map((item) => (
    <TouchableOpacity key={item.id} style={styles.menuItem} ... >
      {/* existing item render */}
    </TouchableOpacity>
  ))}
</View>
```

Add spacing between sections and action items if needed (marginTop on action items container).
</action>
<verify>npx expo start --ios (dev server starts), navigate to Settings, verify section headers display correctly</verify>
<done>SettingsScreen shows section headers for Account, Privacy, Legal, Support, with Sign Out/Delete at bottom</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SettingsScreen loads without errors
- [ ] Four section headers visible: Account, Privacy, Legal, Support
- [ ] Menu items correctly grouped under their sections
- [ ] Sign Out and Delete Account at bottom without header
- [ ] Section headers match dark theme (uppercase, secondary text color)
- [ ] No TypeScript/ESLint errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Settings page visually organized into clear sections
- Section headers match existing app aesthetic
  </success_criteria>

<output>
After completion, create `.planning/phases/31-settings-section-headers/31-01-SUMMARY.md`:

# Phase 31 Plan 01: Settings Section Headers Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `src/screens/SettingsScreen.js` - Added section headers

## Decisions Made

[Key decisions or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 31 complete (single plan). Milestone complete!
</output>
