---
phase: 30-rewind-rebrand
plan: 03
type: execute
---

<objective>
Update app configuration with Rewind identity - name, bundle IDs, colors, and permission strings.

Purpose: Complete the identity transition in app.json and find/replace any hardcoded "Oly" references.
Output: Fully rebranded app.json and codebase with no "Oly" references remaining.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-rewind-rebrand/30-CONTEXT.md
@app.json

**Current app.json values to change:**

- name: "Oly" → "Rewind"
- slug: "Oly" → "Rewind"
- userInterfaceStyle: "light" → "dark"
- splash.backgroundColor: "#FAFAFA" → "#0F0F0F"
- ios.bundleIdentifier: "com.spoodsjs.oly" → "com.spoodsjs.rewind"
- android.adaptiveIcon.backgroundColor: "#ffffff" → "#0F0F0F"
- android.package: "com.oly.app" → "com.spoodsjs.rewind"
- Camera permission text: "Oly" → "Rewind"

**Note:** The CFBundleURLSchemes for Google OAuth must NOT change - those are tied to the OAuth client ID.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.json with Rewind identity</name>
  <files>app.json</files>
  <action>
Update app.json with the following changes:

1. Basic identity:
   - "name": "Rewind"
   - "slug": "Rewind"
   - "userInterfaceStyle": "dark"

2. Splash screen:
   - "splash.backgroundColor": "#0F0F0F"

3. iOS configuration:
   - "ios.bundleIdentifier": "com.spoodsjs.rewind"
   - Keep infoPlist.CFBundleURLSchemes unchanged (OAuth)
   - Keep aps-environment as "development"

4. Android configuration:
   - "android.adaptiveIcon.backgroundColor": "#0F0F0F"
   - "android.package": "com.spoodsjs.rewind"

5. Camera permission (in plugins array):
   - Update cameraPermission text from "Allow Oly to access your camera..." to "Allow Rewind to access your camera to capture moments."

Do NOT change:

- expo.extra.eas.projectId (keep existing)
- expo.owner (keep "spoodsjs")
- CFBundleURLSchemes (OAuth client ID specific)
- Firebase plugin configuration
  </action>
  <verify>
  grep -E "(Rewind|0F0F0F|spoodsjs.rewind)" app.json | head -10
  </verify>
  <done>app.json has Rewind name, dark theme, correct bundle IDs, updated permission strings</done>
  </task>

<task type="auto">
  <name>Task 2: Find and replace hardcoded "Oly" references in codebase</name>
  <files>Multiple source files (search and update)</files>
  <action>
Search the codebase for any remaining "Oly" references:

1. Search for "Oly" in src/ directory:

   ```bash
   grep -r "Oly" src/ --include="*.js" --include="*.jsx"
   ```

2. Search in root-level files:

   ```bash
   grep -l "Oly" *.md *.json 2>/dev/null
   ```

3. For each file found, update "Oly" to "Rewind" where appropriate.

Expected places to check:

- Components that might display app name
- Comments or documentation strings
- Any hardcoded references

Do NOT change:

- Git history references
- CLAUDE.md historical documentation (unless referring to current app name)
- Any OAuth/Firebase configuration strings

If no hardcoded "Oly" references are found in source code, report that clearly.
</action>
<verify>
grep -r "Oly" src/ --include="_.js" --include="_.jsx" | grep -v node_modules || echo "No Oly references in src/"
</verify>
<done>No hardcoded "Oly" references remain in source code (or all found references have been updated)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] app.json shows name "Rewind" and slug "Rewind"
- [ ] app.json splash.backgroundColor is "#0F0F0F"
- [ ] app.json ios.bundleIdentifier is "com.spoodsjs.rewind"
- [ ] app.json android.package is "com.spoodsjs.rewind"
- [ ] No "Oly" references in src/*.js files (except historical comments if any)
- [ ] `npx expo config --type public | grep -i rewind` shows app name
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- App identity fully transitioned to Rewind
- No syntax errors in app.json
  </success_criteria>

<output>
After completion, create `.planning/phases/30-rewind-rebrand/30-03-SUMMARY.md`
</output>
