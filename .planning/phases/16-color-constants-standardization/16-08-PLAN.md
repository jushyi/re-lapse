---
phase: 16-color-constants-standardization
plan: 08
type: execute
---

<objective>
Update card and display components to use color constants.

Purpose: Standardize card components that appear throughout the app.
Output: Album cards, Selects components, and Feed/Friend cards using color constants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-color-constants-standardization/16-CONTEXT.md
@.planning/phases/16-color-constants-standardization/16-01-SUMMARY.md

@src/constants/colors.js
@src/components/AlbumCard.js
@src/components/AlbumBar.js
@src/components/AlbumPhotoViewer.js
@src/components/MonthlyAlbumCard.js
@src/components/SelectsEditOverlay.js
@src/components/FullscreenSelectsViewer.js
@src/components/FriendCard.js
@src/components/TakeFirstPhotoCard.js
@src/components/FeedLoadingSkeleton.js

**Color mapping reference:**

- '#2A2A2A' → colors.background.tertiary
- '#22C55E' → colors.status.ready
- '#FFFFFF' → colors.text.primary or colors.icon.primary
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update AlbumCard.js, AlbumBar.js, AlbumPhotoViewer.js, and MonthlyAlbumCard.js</name>
  <files>src/components/AlbumCard.js, src/components/AlbumBar.js, src/components/AlbumPhotoViewer.js, src/components/MonthlyAlbumCard.js</files>
  <action>
For each file:
1. Add import: `import { colors } from '../constants/colors';`

2. Replace hardcoded colors:
   - Card backgrounds (e.g., '#2A2A2A') → colors.background.tertiary
   - Text colors → colors.text.\*
   - Border colors → colors.border.\*

For AlbumCard.js specifically:

- backgroundColor: '#2A2A2A' → backgroundColor: colors.background.tertiary

For AlbumPhotoViewer.js:

- Ensure full-screen viewer has proper dark background

Search for all # hex patterns and replace.
</action>
<verify>Album cards display correctly; Album bar scrolls; Photo viewer works</verify>
<done>Album card components use color constants</done>
</task>

<task type="auto">
  <name>Task 2: Update SelectsEditOverlay.js and FullscreenSelectsViewer.js</name>
  <files>src/components/SelectsEditOverlay.js, src/components/FullscreenSelectsViewer.js</files>
  <action>
For each file:
1. Add import: `import { colors } from '../constants/colors';`

2. Replace hardcoded colors:
   - Overlay backgrounds → colors.background.primary or colors.overlay.dark
   - Text colors → colors.text.\*
   - Delete bar/danger colors → colors.status.danger

Search for all # hex patterns and replace.
</action>
<verify>Selects edit overlay works; Fullscreen viewer displays correctly</verify>
<done>Selects components use color constants</done>
</task>

<task type="auto">
  <name>Task 3: Update FriendCard.js, TakeFirstPhotoCard.js, and FeedLoadingSkeleton.js</name>
  <files>src/components/FriendCard.js, src/components/TakeFirstPhotoCard.js, src/components/FeedLoadingSkeleton.js</files>
  <action>
For FriendCard.js:
1. Add import: `import { colors } from '../constants/colors';`
2. Replace colors (note: FriendCard.styles.js will be updated in Plan 9)
   - '#22C55E' → colors.status.ready
   - '#FFFFFF' → colors.text.primary

For TakeFirstPhotoCard.js:

1. Add import: `import { colors } from '../constants/colors';`
2. Replace all hardcoded colors

For FeedLoadingSkeleton.js:

1. Add import: `import { colors } from '../constants/colors';`
2. Replace skeleton placeholder colors

Search for all # hex patterns and replace.
</action>
<verify>Friend cards display correctly; Empty state card works; Loading skeleton animates</verify>
<done>FriendCard.js, TakeFirstPhotoCard.js, and FeedLoadingSkeleton.js use color constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All album card components display correctly
- [ ] Selects edit overlay works (drag, delete)
- [ ] Friend cards show correct styling
- [ ] Empty state cards appear correctly
- [ ] Loading skeleton is visible
- [ ] No hardcoded hex values remain
</verification>

<success_criteria>

- All 9 card/display components use color constants
- No hardcoded hex values
- Visual appearance maintained
- All interactions work correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/16-color-constants-standardization/16-08-SUMMARY.md`
</output>
