---
phase: 16-color-constants-standardization
plan: 05
type: execute
---

<objective>
Update Album and Selects screens to use color constants.

Purpose: Standardize the album management and selects screens for consistent appearance.
Output: AlbumGridScreen.js, AlbumPhotoPickerScreen.js, and SelectsScreen.js using color constants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-color-constants-standardization/16-CONTEXT.md
@.planning/phases/16-color-constants-standardization/16-01-SUMMARY.md

@src/constants/colors.js
@src/screens/AlbumGridScreen.js
@src/screens/AlbumPhotoPickerScreen.js
@src/screens/SelectsScreen.js

**Color mapping reference:**

- '#000000' → colors.background.primary
- '#333333' → colors.border.subtle or colors.background.tertiary
- '#888888' → colors.text.secondary
- '#FFFFFF' → colors.text.primary or colors.icon.primary
- '#4CD964' → colors.status.ready (iOS green)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update AlbumGridScreen.js and AlbumPhotoPickerScreen.js</name>
  <files>src/screens/AlbumGridScreen.js, src/screens/AlbumPhotoPickerScreen.js</files>
  <action>
For AlbumGridScreen.js:
1. Add import: `import { colors } from '../constants/colors';`

2. Replace hardcoded colors:
   - backgroundColor: '#333333' → backgroundColor: colors.background.tertiary
   - color: '#FFFFFF' → color: colors.text.primary
   - Ionicons color="#4CD964" → color={colors.status.ready}
   - Any other hex values → appropriate constants

For AlbumPhotoPickerScreen.js:

1. Add import: `import { colors } from '../constants/colors';`

2. Replace hardcoded colors:
   - color: '#888888' → color: colors.text.secondary
   - Any other hex values → appropriate constants

Search both files for all # hex patterns and replace.

**Fix white flash issue:** Ensure both screens' outermost containers have `flex: 1` and `backgroundColor: colors.background.primary`.
</action>
<verify>Album grid displays correctly; photo picker works; selection checkmarks visible; no white flash at edges during navigation</verify>
<done>AlbumGridScreen.js and AlbumPhotoPickerScreen.js use color constants</done>
</task>

<task type="auto">
  <name>Task 2: Update SelectsScreen.js</name>
  <files>src/screens/SelectsScreen.js</files>
  <action>
1. Add import: `import { colors } from '../constants/colors';`

2. Replace all hardcoded colors:
   - Background colors → colors.background.\*
   - Text colors → colors.text.\*
   - Border colors → colors.border.\*
   - Any brand/accent colors → colors.brand.\*

3. Search for all # hex patterns and replace with constants.

**Fix white flash issue:** Ensure outermost container has `flex: 1` and `backgroundColor: colors.background.primary`.

Do NOT change logic or layout.
</action>
<verify>Selects screen loads; thumbnails display correctly; drag-to-reorder works; no white flash at edges</verify>
<done>SelectsScreen.js uses color constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AlbumGridScreen loads without errors
- [ ] AlbumPhotoPickerScreen loads without errors
- [ ] SelectsScreen loads without errors
- [ ] Photo selection works correctly
- [ ] No hardcoded hex values remain in any of the three files
</verification>

<success_criteria>

- AlbumGridScreen.js uses color constants
- AlbumPhotoPickerScreen.js uses color constants
- SelectsScreen.js uses color constants
- No hardcoded hex values
- Visual appearance maintained
  </success_criteria>

<output>
After completion, create `.planning/phases/16-color-constants-standardization/16-05-SUMMARY.md`
</output>
