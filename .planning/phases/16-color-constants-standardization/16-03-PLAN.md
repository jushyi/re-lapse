---
phase: 16-color-constants-standardization
plan: 03
type: execute
---

<objective>
Update Profile and Activity screens to use color constants.

Purpose: Standardize the profile and activity/notification screens for consistent appearance.
Output: ProfileScreen.js, ActivityScreen.js, and NotificationsScreen.js using color constants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-color-constants-standardization/16-CONTEXT.md
@.planning/phases/16-color-constants-standardization/16-01-SUMMARY.md

@src/constants/colors.js
@src/screens/ProfileScreen.js
@src/screens/ActivityScreen.js
@src/screens/NotificationsScreen.js

**Color mapping reference:**

- '#000000' → colors.background.primary
- '#FFFFFF' → colors.text.primary or colors.icon.primary
- '#888888' → colors.text.secondary
- '#FAFAFA' → colors.background.white (light backgrounds)
- '#22C55E' → colors.status.ready
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProfileScreen.js and ActivityScreen.js</name>
  <files>src/screens/ProfileScreen.js, src/screens/ActivityScreen.js</files>
  <action>
For each file:

1. Add import: `import { colors } from '../constants/colors';`

2. Replace all hardcoded hex colors:
   - Background colors → colors.background.\*
   - Text colors → colors.text.\*
   - Icon colors in Ionicons → colors.icon.\*

3. In ActivityScreen.js specifically:
   - color: '#FFFFFF' → color: colors.text.primary
   - Any checkmark/icon colors → colors.icon.primary or colors.status.ready

4. Search entire file for # followed by hex digits and replace each instance.

**Fix white flash issue:** Ensure each screen's outermost container has BOTH:

- `flex: 1`
- `backgroundColor: colors.background.primary`

Do NOT change logic or layout.
</action>
<verify>Both screens load and display correctly; no hardcoded colors remain; no white flash at edges during navigation</verify>
<done>ProfileScreen.js and ActivityScreen.js use color constants throughout</done>
</task>

<task type="auto">
  <name>Task 2: Update NotificationsScreen.js</name>
  <files>src/screens/NotificationsScreen.js</files>
  <action>
1. Add import: `import { colors } from '../constants/colors';`

2. Replace all hardcoded colors:
   - '#FAFAFA' → colors.background.white (or if this should be dark, use colors.background.primary)
   - Other colors → appropriate constants

3. Note: If NotificationsScreen has a light background (#FAFAFA), consider whether this should be changed to dark theme (colors.background.primary) for consistency. If keeping light, use colors.background.white.

4. Replace all other hex values with constants.

**Fix white flash issue:** Ensure outermost container has `flex: 1` and `backgroundColor: colors.background.primary`.

Do NOT change logic.
</action>
<verify>NotificationsScreen loads correctly; colors are consistent; no white flash at edges</verify>
<done>NotificationsScreen.js uses color constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ProfileScreen loads without errors
- [ ] ActivityScreen loads without errors
- [ ] NotificationsScreen loads without errors
- [ ] All three screens have consistent styling
- [ ] No hardcoded hex values remain
</verification>

<success_criteria>

- ProfileScreen.js uses color constants
- ActivityScreen.js uses color constants
- NotificationsScreen.js uses color constants
- No hardcoded hex values in any of these files
- Screens display correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/16-color-constants-standardization/16-03-SUMMARY.md`
</output>
