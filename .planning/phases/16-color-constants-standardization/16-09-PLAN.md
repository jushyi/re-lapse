---
phase: 16-color-constants-standardization
plan: 09
type: execute
---

<objective>
Update style files and remaining miscellaneous files to use color constants.

Purpose: Complete the color standardization by updating all style modules and remaining files.
Output: All style files and misc files using color constants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-color-constants-standardization/16-CONTEXT.md
@.planning/phases/16-color-constants-standardization/16-01-SUMMARY.md

@src/constants/colors.js

Style files to update:

- src/styles/FeedPhotoCard.styles.js
- src/styles/SwipeablePhotoCard.styles.js
- src/styles/PhotoDetailModal.styles.js
- src/styles/PhotoDetailScreen.styles.js
- src/styles/FriendCard.styles.js
- src/styles/FriendsScreen.styles.js

Misc files to update:

- src/components/Card.js
- src/components/ErrorBoundary.js
- src/components/ReactionDisplay.js
- src/context/ThemeContext.js
- src/services/firebase/notificationService.js (if has colors)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update all style files</name>
  <files>src/styles/FeedPhotoCard.styles.js, src/styles/SwipeablePhotoCard.styles.js, src/styles/PhotoDetailModal.styles.js, src/styles/PhotoDetailScreen.styles.js, src/styles/FriendCard.styles.js, src/styles/FriendsScreen.styles.js</files>
  <action>
For each style file:
1. Add import: `import { colors } from '../constants/colors';`

2. Replace all hardcoded colors:
   - backgroundColor values → colors.background.\*
   - color values → colors.text.\*
   - borderColor values → colors.border.\*
   - Any rgba overlays → colors.overlay.\*

Specific mappings:

- '#000000' → colors.background.primary
- '#2A2A2A' → colors.background.tertiary
- '#333333' → colors.border.subtle or colors.background.tertiary
- '#3A3A3A', '#4A4A4A' → colors.border.subtle (or add to colors.js if needed)
- '#FFFFFF' → colors.text.primary
- '#888888' → colors.text.secondary
- '#CCCCCC' → colors.text.secondary
- '#9370DB' → colors.brand.purple (medium purple)
- '#ffffff57' (semi-transparent white) → 'rgba(255, 255, 255, 0.34)' or add to colors.overlay

If any colors don't have good matches in colors.js, add them to the appropriate section.

Search each file for all # and rgba( patterns.
</action>
<verify>All styled components render correctly; no style errors in console</verify>
<done>All 6 style files use color constants</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining misc files</name>
  <files>src/components/Card.js, src/components/ErrorBoundary.js, src/components/ReactionDisplay.js, src/context/ThemeContext.js</files>
  <action>
For each file:
1. Check if file has hardcoded color values
2. If yes, add import: `import { colors } from '../constants/colors';` (or appropriate path)
3. Replace hardcoded colors with constants

For ThemeContext.js specifically:

- If it defines a theme object, ensure it uses colors from colors.js
- This ensures theme consistency throughout the app

For notificationService.js:

- Check if it has any color values (likely for notification styling)
- If yes, replace with constants

For Card.js and ErrorBoundary.js:

- Replace any hardcoded colors

For ReactionDisplay.js:

- Replace reaction-related colors

Search for # patterns in each file.
</action>
<verify>Components render correctly; no errors; theme applies correctly</verify>
<done>All remaining files use color constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All style files import and use colors correctly
- [ ] No StyleSheet has hardcoded hex values
- [ ] ThemeContext uses colors.js
- [ ] Card components display correctly
- [ ] Error boundary shows correctly (if triggered)
- [ ] Reactions display correctly
- [ ] grep for hardcoded colors shows minimal/no results outside colors.js
</verification>

<success_criteria>

- All style files use color constants
- All remaining component files use color constants
- ThemeContext aligned with colors.js
- No hardcoded hex values outside colors.js (except possibly SVG-specific cases)
- App functions normally
  </success_criteria>

<output>
After completion, create `.planning/phases/16-color-constants-standardization/16-09-SUMMARY.md`
</output>
