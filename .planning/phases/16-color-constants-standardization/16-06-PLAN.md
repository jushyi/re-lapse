---
phase: 16-color-constants-standardization
plan: 06
type: execute
---

<objective>
Update Settings and Auth screens to use color constants.

Purpose: Standardize settings and authentication-related screens for consistent appearance.
Output: SettingsScreen.js, DeleteAccountScreen.js, PrivacyPolicyScreen.js, TermsOfServiceScreen.js, and SuccessScreen.js using color constants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-color-constants-standardization/16-CONTEXT.md
@.planning/phases/16-color-constants-standardization/16-01-SUMMARY.md

@src/constants/colors.js
@src/screens/SettingsScreen.js
@src/screens/DeleteAccountScreen.js
@src/screens/PrivacyPolicyScreen.js
@src/screens/TermsOfServiceScreen.js
@src/screens/SuccessScreen.js

**Color mapping reference:**

- '#000000' → colors.background.primary
- '#1A1A1A' → colors.background.secondary
- '#333333' → colors.border.subtle
- '#666666' → colors.text.tertiary
- '#999999' → colors.text.secondary
- '#CCCCCC' → colors.text.secondary
- '#FFFFFF' → colors.text.primary or colors.icon.primary
- '#FF3B30' → colors.status.danger
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update SettingsScreen.js and DeleteAccountScreen.js</name>
  <files>src/screens/SettingsScreen.js, src/screens/DeleteAccountScreen.js</files>
  <action>
For SettingsScreen.js:
1. Add import: `import { colors } from '../constants/colors';`
2. Replace all hardcoded colors with constants
3. Search for all # hex patterns

For DeleteAccountScreen.js:

1. Add import: `import { colors } from '../constants/colors';`

2. This file has many hardcoded colors in its StyleSheet. Replace all:
   - backgroundColor: '#000000' → backgroundColor: colors.background.primary
   - backgroundColor: '#1A1A1A' → backgroundColor: colors.background.secondary
   - borderBottomColor: '#333333' → borderBottomColor: colors.border.subtle
   - borderColor: '#333333' → borderColor: colors.border.subtle
   - borderColor: '#FF3B30' → borderColor: colors.status.danger
   - color: '#FFFFFF' → color: colors.text.primary
   - color: '#999999' → color: colors.text.secondary
   - color: '#666666' → color: colors.text.tertiary
   - color: '#CCCCCC' → color: colors.text.secondary
   - color: '#FF3B30' → color: colors.status.danger
   - placeholderTextColor="#666666" → placeholderTextColor={colors.text.tertiary}
   - Ionicons color="#FF3B30" → color={colors.status.danger}
   - Ionicons color="#FFFFFF" → color={colors.icon.primary}
   - ActivityIndicator color="#FF3B30" → color={colors.status.danger}

**Fix white flash issue:** Ensure both screens' outermost containers have `flex: 1` and `backgroundColor: colors.background.primary`.

Do NOT change logic.
</action>
<verify>Settings screen loads; Delete account screen displays correctly; danger styling preserved; no white flash at edges</verify>
<done>SettingsScreen.js and DeleteAccountScreen.js use color constants</done>
</task>

<task type="auto">
  <name>Task 2: Update PrivacyPolicyScreen.js, TermsOfServiceScreen.js, and SuccessScreen.js</name>
  <files>src/screens/PrivacyPolicyScreen.js, src/screens/TermsOfServiceScreen.js, src/screens/SuccessScreen.js</files>
  <action>
For each file:
1. Add import: `import { colors } from '../constants/colors';`

2. Replace hardcoded colors:
   - backgroundColor: '#000000' → backgroundColor: colors.background.primary
   - Any text colors → colors.text.\*
   - Any icon colors → colors.icon.\*

3. Search for all # hex patterns and replace.

These screens are similar legal/info screens, so changes should be consistent across all three.

**Fix white flash issue:** Ensure each screen's outermost container has `flex: 1` and `backgroundColor: colors.background.primary`.
</action>
<verify>Privacy policy screen loads; Terms screen loads; Success screen displays correctly; no white flash at edges</verify>
<done>PrivacyPolicyScreen.js, TermsOfServiceScreen.js, and SuccessScreen.js use color constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SettingsScreen loads without errors
- [ ] DeleteAccountScreen loads without errors
- [ ] Delete account danger styling (red) is preserved
- [ ] PrivacyPolicyScreen loads without errors
- [ ] TermsOfServiceScreen loads without errors
- [ ] SuccessScreen loads without errors
- [ ] No hardcoded hex values remain in any files
</verification>

<success_criteria>

- All five screens use color constants
- No hardcoded hex values
- Danger/warning styling preserved (red colors)
- Visual appearance maintained
  </success_criteria>

<output>
After completion, create `.planning/phases/16-color-constants-standardization/16-06-SUMMARY.md`
</output>
