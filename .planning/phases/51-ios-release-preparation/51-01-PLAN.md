---
phase: 51-ios-release-preparation
plan: 01
type: execute
---

<objective>
Rebrand the app from "Rewind" to "Flick" — replace all in-code references, update splash screen text, update app icon, and update legal content.

Purpose: Establish the new "Flick" identity before any App Store submission or production configuration.
Output: All user-facing "Rewind" references replaced with "Flick", new app icon asset, updated splash text.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-ios-release-preparation/51-CONTEXT.md
@.planning/phases/51-ios-release-preparation/51-RESEARCH.md

@app.json
@app.config.js
@src/components/AnimatedSplash.js
@src/screens/FeedScreen.js
@src/screens/CameraScreen.js
@src/screens/SettingsScreen.js
@src/constants/legalContent.js
@src/constants/colors.js
@src/context/ThemeContext.js
@src/services/downloadPhotosService.js

**Rebrand scope (from grep):**
Files containing "Rewind" or "rewind":

- `src/components/AnimatedSplash.js` — "REWIND" splash text + style name `rewindText`
- `src/screens/FeedScreen.js` — header title "Rewind"
- `src/screens/CameraScreen.js` — camera permission text
- `src/screens/SettingsScreen.js` — support email subject + mailto
- `src/constants/legalContent.js` — Privacy Policy + ToS references throughout
- `src/constants/colors.js` — comment "Rewind App Color System"
- `src/context/ThemeContext.js` — storage key `@rewind_theme_palette`
- `src/services/downloadPhotosService.js` — album name "Rewind Export"
- `app.json` — name, slug, camera permission text

**Vision:** Same dark aesthetic, same animations — only names and identity change. Film strip with retro "F" for icon.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all "Rewind" references in source code and config</name>
  <files>app.json, src/components/AnimatedSplash.js, src/screens/FeedScreen.js, src/screens/CameraScreen.js, src/screens/SettingsScreen.js, src/constants/legalContent.js, src/constants/colors.js, src/context/ThemeContext.js, src/services/downloadPhotosService.js</files>
  <action>
Replace all "Rewind" / "REWIND" / "rewind" references with "Flick" / "FLICK" / "flick" across all files:

**app.json:**

- `"name": "Rewind"` → `"name": "Flick"`
- `"slug": "Rewind"` → `"slug": "Flick"`
- Camera permission: "Allow Rewind to access" → "Allow Flick to access"

**AnimatedSplash.js:**

- Text content: `"REWIND"` → `"FLICK"`
- Style name: `rewindText` → `flickText` (and update the JSX reference)

**FeedScreen.js:**

- Header title: `"Rewind"` → `"Flick"`

**CameraScreen.js:**

- Permission text: "Rewind needs access" → "Flick needs access"

**SettingsScreen.js:**

- Support email: `support@rewind.app` → leave as placeholder `support@flickapp.com` (will be updated in 51-04 when domain is chosen)
- Subject: `Rewind%20Support%20Request` → `Flick%20Support%20Request`

**legalContent.js:**

- Replace ALL instances of `Rewind` with `Flick` in both Privacy Policy and Terms of Service content strings. Use replace_all. Keep the operator name "Joshua Jireh Maserin" unchanged.

**colors.js:**

- Comment: "Rewind App Color System" → "Flick App Color System"

**ThemeContext.js:**

- Storage key: `@rewind_theme_palette` → `@flick_theme_palette`

**downloadPhotosService.js:**

- Album name: `"Rewind Export"` → `"Flick Export"` (all 3 occurrences: getAlbumAsync, debug log, createAlbumAsync)

Do NOT change the bundleIdentifier `com.spoodsjs.rewind` — that stays as-is to avoid breaking EAS project linkage. The bundle ID is an internal identifier, not user-facing.
</action>
<verify>Run `grep -r "Rewind\|rewind\|REWIND" src/ app.json --include="*.js" --include="*.json" | grep -v "node_modules" | grep -v "bundleIdentifier" | grep -v "package"` returns no matches (except bundleIdentifier which stays)</verify>
<done>Zero user-facing "Rewind" references remain in source code. Only bundleIdentifier and package name keep "rewind" (internal, not user-facing).</done>
</task>

<task type="auto">
  <name>Task 2: Create new app icon — film strip with retro "F"</name>
  <files>assets/icon.png</files>
  <action>
Create a new 1024x1024 app icon for "Flick" that features:
- A film strip motif (vertical perforations on sides)
- A retro-styled capital "F" in the center
- Dark aesthetic matching the app (dark background, likely using the app's cyan/amber accent colors)
- Clean, recognizable at small sizes (iOS renders at 60x60 on home screen)

Use a programmatic approach: create an SVG or use a canvas-based Node script to generate the icon. Export as 1024x1024 PNG to `assets/icon.png` (overwriting the existing one). Also update `assets/adaptive-icon.png` with the same design for Android consistency.

The icon should feel retro/analog (matching the disposable camera theme) but clean and modern. Dark background (#0A0A1A or similar), with the film strip and F in lighter colors.
</action>
<verify>File `assets/icon.png` exists and is 1024x1024 PNG. File `assets/adaptive-icon.png` updated.</verify>
<done>New Flick app icon created with film strip + retro F design, saved as assets/icon.png and assets/adaptive-icon.png.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete rebrand from "Rewind" to "Flick" — all in-code text, splash screen, feed header, legal content, and new app icon</what-built>
  <how-to-verify>
    1. Run: `npx expo start`
    2. Open app on iOS simulator or device
    3. Verify splash screen shows "FLICK" (not "REWIND")
    4. Verify feed header shows "Flick" (not "Rewind")
    5. Navigate to Settings → Help & Support → confirm email subject says "Flick"
    6. Navigate to Settings → Privacy Policy → confirm text says "Flick" throughout
    7. Navigate to Settings → Terms of Service → confirm text says "Flick" throughout
    8. Check the app icon in the simulator home screen — should show the new film strip + F design
    9. Confirm no "Rewind" text appears anywhere in the app
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "Rewind\|REWIND" src/ app.json --include="*.js" --include="*.json" | grep -v bundleIdentifier | grep -v package` returns empty
- [ ] App launches without errors
- [ ] Splash shows "FLICK"
- [ ] Feed header shows "Flick"
- [ ] New app icon renders correctly
</verification>

<success_criteria>

- All user-facing "Rewind" references replaced with "Flick"
- New app icon created and configured
- Legal content updated
- App launches and renders correctly
- Human verification approved
  </success_criteria>

<output>
After completion, create `.planning/phases/51-ios-release-preparation/51-01-SUMMARY.md`
</output>
