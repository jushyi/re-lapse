---
phase: 29-settings-help
plan: 01
type: execute
---

<objective>
Add Help/Support email link and app version display to Settings screen.

Purpose: Give users a clear way to reach support and identify their app version - essential for troubleshooting and user trust.
Output: Settings screen with Help/Support and Version rows, following existing styling patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-settings-help/29-CONTEXT.md

# Key files:

@src/screens/SettingsScreen.js
@src/constants/colors.js

# Pattern reference (Linking usage):

@src/services/firebase/contactSyncService.js (lines 30, 103 - shows Linking.openURL pattern)

**Scope from CONTEXT.md:**

- Help/Support link: Opens email compose with support address pre-filled
- App version display: Row at very bottom showing version number
- Out of scope: Notification preferences, clear cache, complex settings sections
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add Help/Support email link</name>
  <files>src/screens/SettingsScreen.js</files>
  <action>
Add a "Help & Support" menu item that opens the user's email app with the support address pre-filled.

Implementation:

1. Import `Linking` from 'react-native' (already imported Alert, add Linking)
2. Add `handleHelpSupport` function that calls `Linking.openURL('mailto:support@rewind.app?subject=Rewind%20Support%20Request')`
3. Add Help & Support menu item object with:
   - id: 'helpSupport'
   - label: 'Help & Support'
   - icon: 'help-circle-outline'
   - onPress: handleHelpSupport
4. Place it in menuItems array AFTER 'Blocked Users' and BEFORE 'Sign Out'
5. Use consistent colors.icon.primary for the icon (not danger, not purple)
   </action>
   <verify>Run app, navigate to Settings, tap "Help & Support" - email app should open with pre-filled support address</verify>
   <done>Help & Support row appears in Settings, tapping opens email compose with mailto:support@rewind.app</done>
   </task>

<task type="auto">
  <name>Task 2: Add app version display</name>
  <files>src/screens/SettingsScreen.js, package.json</files>
  <action>
Add app version display at the very bottom of Settings screen.

Implementation:

1. Install expo-application: `npx expo install expo-application`
2. Import at top of SettingsScreen.js: `import * as Application from 'expo-application';`
3. Add version row as a separate section at the bottom (not in menuItems array - it's display-only, not tappable):
   - Create a new View after the menuContainer
   - Style: paddingHorizontal: 20, paddingVertical: 24, alignItems: 'center'
   - Text shows: `Version ${Application.nativeApplicationVersion || '0.1.0'}`
   - Text style: colors.text.tertiary, fontSize: 14
4. Fallback to hardcoded '0.1.0' in case nativeApplicationVersion is null (dev builds)

Note: Do NOT make the version row tappable - it's just informational display.
</action>
<verify>Run app, navigate to Settings, scroll to bottom - version number should be visible in muted text</verify>
<done>Version displayed at bottom of Settings in muted text (colors.text.tertiary), showing "Version X.X.X"</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Help & Support row appears in Settings menu
- [ ] Tapping Help & Support opens email compose
- [ ] Version number displayed at bottom of Settings
- [ ] All styling uses color constants (no hardcoded values)
- [ ] `npm run lint` passes
</verification>

<success_criteria>

- Help & Support row present and functional (opens mailto: link)
- App version displayed at bottom of Settings
- Consistent styling with existing Settings items
- No new warnings or errors
  </success_criteria>

<output>
After completion, create `.planning/phases/29-settings-help/29-01-SUMMARY.md` using summary.md template.
</output>
