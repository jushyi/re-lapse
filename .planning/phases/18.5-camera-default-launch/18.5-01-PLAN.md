---
phase: 18.5-camera-default-launch
plan: 01
type: execute
---

<objective>
Make Camera the default screen on app launch instead of Feed.

Purpose: Align with the app's capture-first philosophy - users should land directly on the camera to encourage photo taking.
Output: App launches to Camera tab instead of Feed tab.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key file:**
@src/navigation/AppNavigator.js

**Tech context:**
- React Navigation 7.x with createBottomTabNavigator
- Tab order is currently: Feed, Camera, Friends, Profile
- Tab.Navigator uses default initialRouteName (first tab = Feed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set Camera as initial route in MainTabNavigator</name>
  <files>src/navigation/AppNavigator.js</files>
  <action>
Add `initialRouteName: 'Camera'` to the Tab.Navigator screenOptions in MainTabNavigator.

The change is on the Tab.Navigator component (around line 74):
```jsx
<Tab.Navigator
  initialRouteName="Camera"  // ADD THIS LINE
  screenOptions={{
    // existing options...
  }}
>
```

This single property change makes Camera the default landing screen while preserving:
- All tab order (Feed, Camera, Friends, Profile)
- All navigation deep linking
- All existing tab styling and icons
</action>
  <verify>
1. Run `npx expo start` and launch on iOS device/simulator
2. Observe app launches directly to Camera tab (camera viewfinder visible)
3. Feed tab icon should NOT be highlighted on launch
4. Camera tab icon SHOULD be highlighted on launch
  </verify>
  <done>App launches to Camera tab by default. Camera icon is active, viewfinder is visible on first screen.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] App launches directly to Camera screen (not Feed)
- [ ] Camera tab icon is highlighted/active on launch
- [ ] All tabs still navigable (Feed, Camera, Friends, Profile)
- [ ] Deep linking still works (`lapse://camera`, `lapse://feed`, etc.)
- [ ] No TypeScript/ESLint errors
</verification>

<success_criteria>

- Camera is the default landing screen on app launch
- Tab navigation unchanged (all tabs work normally)
- Deep linking unchanged (all routes still function)
- No regressions to navigation behavior
</success_criteria>

<output>
After completion, create `.planning/phases/18.5-camera-default-launch/18.5-01-SUMMARY.md`:

# Phase 18.5 Plan 01: Camera Default Launch Screen Summary

**[One-liner describing what shipped]**

## Accomplishments

- [Key outcome]

## Files Modified

- `src/navigation/AppNavigator.js` - Added initialRouteName to Tab.Navigator

## Decisions Made

None - straightforward configuration change.

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for Phase 18.6 (Triage Animation Timing Optimization)
</output>
