---
phase: 48-ui-ux-consistency-audit
plan: 02
type: execute
---

<objective>
Audit and fix settings and account management screens for UI/UX consistency.

Purpose: Settings and secondary screens were identified as needing the most attention. Ensure consistent styling, toggles, list items, section headers, and button patterns across all settings/account screens.
Output: All settings/account screens using constants consistently, matching established dark theme.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-ui-ux-consistency-audit/48-CONTEXT.md
@.planning/phases/48-ui-ux-consistency-audit/48-01-SUMMARY.md

# Design system references:

@src/constants/colors.js
@src/constants/spacing.js
@src/constants/typography.js
@src/constants/layout.js

# Screens to audit:

@src/screens/SettingsScreen.js
@src/screens/NotificationSettingsScreen.js
@src/screens/BlockedUsersScreen.js
@src/screens/EditProfileScreen.js
@src/screens/DeleteAccountScreen.js
@src/screens/ReportUserScreen.js
@src/screens/PrivacyPolicyScreen.js
@src/screens/TermsOfServiceScreen.js

# Style files:

@src/styles/BlockedUsersScreen.styles.js
@src/styles/NotificationSettingsScreen.styles.js
@src/styles/ReportUserScreen.styles.js

**Audit checklist per file:**

1. No hardcoded hex/rgb/rgba color values — use colors.\*
2. No hardcoded numeric padding/margin — use spacing.\*
3. No hardcoded font families — use typography.fontFamily.\*
4. No hardcoded font sizes — use typography.size.\*
5. No hardcoded border radius — use layout.borderRadius.\*
6. No hardcoded dimensions — use layout.dimensions.\*
7. Consistent section headers, toggles, list items, destructive buttons
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix settings screens (SettingsScreen, NotificationSettingsScreen, BlockedUsersScreen)</name>
  <files>src/screens/SettingsScreen.js, src/screens/NotificationSettingsScreen.js, src/screens/BlockedUsersScreen.js, src/styles/BlockedUsersScreen.styles.js, src/styles/NotificationSettingsScreen.styles.js</files>
  <action>
Read each screen and its style file. Apply the full audit checklist:
1. Replace hardcoded colors → colors.* constants
2. Replace hardcoded spacing → spacing.* constants
3. Replace hardcoded fonts → typography.* constants
4. Replace hardcoded borderRadius → layout.borderRadius.*
5. Replace hardcoded dimensions → layout.dimensions.*

Cross-screen consistency checks:

- Section headers should use the same font (typography.fontFamily.body or bodyBold), same size, same color across all settings screens
- Toggle switches should use consistent active/inactive colors
- List item rows should use the same height, padding, and separator styling
- Screen backgrounds must be colors.background.primary
- Cards/sections must use colors.background.secondary
- Destructive actions (delete, block) should use colors.status.danger consistently

Do NOT change layouts or functionality. Only standardize to constants and ensure cross-screen consistency.
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>SettingsScreen, NotificationSettingsScreen, BlockedUsersScreen all use design system constants with consistent section headers, toggles, and list patterns</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix account management and legal screens (EditProfileScreen, DeleteAccountScreen, ReportUserScreen, legal screens)</name>
  <files>src/screens/EditProfileScreen.js, src/screens/DeleteAccountScreen.js, src/screens/ReportUserScreen.js, src/screens/PrivacyPolicyScreen.js, src/screens/TermsOfServiceScreen.js, src/styles/ReportUserScreen.styles.js</files>
  <action>
Read each screen and apply the same audit checklist.

Specific checks:

- EditProfileScreen: Input fields should match the Input component's styling pattern. Save/cancel buttons should use standard Button component patterns.
- DeleteAccountScreen: Destructive button must use colors.status.danger. Warning text should use colors.status.warning. Grace period info should use colors.text.secondary.
- ReportUserScreen: Form elements should match established input/button patterns. Report reasons should use consistent list styling.
- PrivacyPolicyScreen/TermsOfServiceScreen: Scroll containers should use colors.background.primary. Text should use colors.text.primary/secondary with consistent typography. Links should use colors.interactive.primary.

Replace all hardcoded values with constants. Ensure consistency between these screens and the settings screens fixed in Task 1.
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>EditProfileScreen, DeleteAccountScreen, ReportUserScreen, PrivacyPolicyScreen, TermsOfServiceScreen all use design system constants with consistent patterns matching other settings screens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Standardized all settings and account management screens to use design system constants. Ensured cross-screen consistency for section headers, toggles, list items, destructive buttons, and form elements.</what-built>
  <how-to-verify>
    1. Run: npx expo start
    2. Navigate to Settings screen — check section headers, list items, toggles
    3. Open Notification Settings — check toggle consistency with main Settings
    4. Open Blocked Users — check list styling matches Friends patterns
    5. Open Edit Profile — check input fields and save button consistency
    6. Open Delete Account — check destructive button styling (red), warning text
    7. Open Report User — check form consistency
    8. Open Privacy Policy / Terms of Service — check text styling and scroll behavior
    9. Confirm: All screens look identical to before (no visual regressions)
    10. Check: Consistent patterns across all settings/account screens
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual regressions or inconsistencies to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx expo lint` passes without errors
- [ ] All settings/account screens use design system constants
- [ ] Section headers consistent across all settings screens
- [ ] Toggles use consistent active/inactive colors
- [ ] Destructive actions use colors.status.danger
- [ ] Visual appearance unchanged (verified by human)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No visual regressions
- 8 screens standardized with cross-screen consistency
  </success_criteria>

<output>
After completion, create `.planning/phases/48-ui-ux-consistency-audit/48-02-SUMMARY.md`
</output>
