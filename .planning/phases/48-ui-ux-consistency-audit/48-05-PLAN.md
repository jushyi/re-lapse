---
phase: 48-ui-ux-consistency-audit
plan: 05
type: execute
---

<objective>
Audit and fix album, photo, and selects screens and components for UI/UX consistency.

Purpose: Ensure all album-related screens and components use consistent grid layouts, card styling, photo viewers, and modal patterns with design system constants.
Output: All album/photo/selects screens and components using constants consistently.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-ui-ux-consistency-audit/48-CONTEXT.md
@.planning/phases/48-ui-ux-consistency-audit/48-04-SUMMARY.md

# Design system references:

@src/constants/colors.js
@src/constants/spacing.js
@src/constants/typography.js
@src/constants/layout.js

# Screens to audit:

@src/screens/AlbumGridScreen.js
@src/screens/CreateAlbumScreen.js
@src/screens/MonthlyAlbumGridScreen.js
@src/screens/RecentlyDeletedScreen.js

# Style files:

@src/styles/RecentlyDeletedScreen.styles.js

# Components to audit:

@src/components/AlbumCard.js
@src/components/AlbumBar.js
@src/components/AlbumPhotoViewer.js
@src/components/MonthlyAlbumCard.js
@src/components/MonthlyAlbumsSection.js
@src/components/YearSection.js
@src/components/SelectsBanner.js
@src/components/SelectsEditOverlay.js
@src/components/FullscreenSelectsViewer.js
@src/components/AddToAlbumSheet.js
@src/components/RenameAlbumModal.js
@src/components/DeletionRecoveryModal.js

**Audit checklist per file:**

1. No hardcoded hex/rgb/rgba color values — use colors.\*
2. No hardcoded numeric padding/margin — use spacing.\*
3. No hardcoded font families — use typography.fontFamily.\*
4. No hardcoded font sizes — use typography.size.\*
5. No hardcoded border radius — use layout.borderRadius.\*
6. No hardcoded dimensions — use layout.dimensions.\*
7. Consistent grid gaps, card styling, modal patterns
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix album screens (AlbumGridScreen, CreateAlbumScreen, MonthlyAlbumGridScreen, RecentlyDeletedScreen)</name>
  <files>src/screens/AlbumGridScreen.js, src/screens/CreateAlbumScreen.js, src/screens/MonthlyAlbumGridScreen.js, src/screens/RecentlyDeletedScreen.js, src/styles/RecentlyDeletedScreen.styles.js</files>
  <action>
Read each screen and style file. Apply the full audit checklist:
1. Replace hardcoded colors → colors.* constants
2. Replace hardcoded spacing → spacing.* constants
3. Replace hardcoded fonts → typography.* constants
4. Replace hardcoded borderRadius → layout.borderRadius.*
5. Replace hardcoded dimensions → layout.dimensions.*

Cross-screen consistency checks:

- Grid layouts (AlbumGridScreen, MonthlyAlbumGridScreen) should use the same gap spacing, column count calculation, and item sizing approach
- Screen headers should use consistent typography and spacing
- Empty states should use consistent messaging patterns
- CreateAlbumScreen: form inputs and buttons should match established Input/Button patterns
- RecentlyDeletedScreen: deletion countdown/info should use colors.status.warning or colors.text.secondary consistently. Recovery buttons should match standard button patterns.

Do NOT change layouts or functionality. Only standardize to constants.
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>AlbumGridScreen, CreateAlbumScreen, MonthlyAlbumGridScreen, RecentlyDeletedScreen all use design system constants with consistent grid and form patterns</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix album/selects components (AlbumCard, AlbumBar, MonthlyAlbumCard, SelectsBanner, modals, and related)</name>
  <files>src/components/AlbumCard.js, src/components/AlbumBar.js, src/components/AlbumPhotoViewer.js, src/components/MonthlyAlbumCard.js, src/components/MonthlyAlbumsSection.js, src/components/YearSection.js, src/components/SelectsBanner.js, src/components/SelectsEditOverlay.js, src/components/FullscreenSelectsViewer.js, src/components/AddToAlbumSheet.js, src/components/RenameAlbumModal.js, src/components/DeletionRecoveryModal.js</files>
  <action>
Read each component. Apply the full audit checklist.

Specific checks:

- AlbumCard: Card background, text colors, border radius, shadow should match established card patterns (colors.background.secondary, layout.borderRadius.md/lg)
- AlbumBar: Horizontal scroll section should use consistent spacing between items
- AlbumPhotoViewer: Overlay colors should use colors.overlay.\*, controls should match standard patterns
- MonthlyAlbumCard/MonthlyAlbumsSection/YearSection: Should use consistent section header styling and card patterns matching AlbumCard
- SelectsBanner/SelectsEditOverlay/FullscreenSelectsViewer: Slideshow UI should use consistent overlay and control styling
- AddToAlbumSheet: Bottom sheet should use colors.background.secondary, consistent list items
- RenameAlbumModal: Input and buttons should match standard patterns
- DeletionRecoveryModal: Warning styling, confirmation buttons should match established patterns

Ensure all card components have consistent styling (same background, border radius, shadow, padding).
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>All 12 album/selects components use design system constants with consistent card patterns, overlay styling, and modal patterns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Standardized all album, photo, and selects screens and components to use design system constants. Ensured consistent grid layouts, card styling, modal patterns, and overlay colors.</what-built>
  <how-to-verify>
    1. Run: npx expo start
    2. Navigate to Profile → Albums section — check album cards styling
    3. Tap into an album → AlbumGridScreen — check grid layout, header
    4. Create new album → check form styling
    5. Check monthly albums section — card styling matches user albums
    6. Navigate to Recently Deleted — check countdown info and recovery buttons
    7. Check Selects banner on profile — slideshow controls, edit overlay
    8. Long-press album → Rename modal — check input/button styling
    9. Try Add to Album sheet — check list styling
    10. Confirm: All screens look identical to before (no visual regressions)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual regressions or inconsistencies to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx expo lint` passes without errors
- [ ] All album/photo/selects screens and components use design system constants
- [ ] Grid layouts consistent between AlbumGridScreen and MonthlyAlbumGridScreen
- [ ] Card styling consistent across AlbumCard, MonthlyAlbumCard
- [ ] Modals and sheets use consistent patterns
- [ ] Visual appearance unchanged (verified by human)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No visual regressions
- 4 screens + 12 components standardized with cross-component consistency
  </success_criteria>

<output>
After completion, create `.planning/phases/48-ui-ux-consistency-audit/48-05-SUMMARY.md`
</output>
