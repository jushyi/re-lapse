---
phase: 48-ui-ux-consistency-audit
plan: 03
type: execute
---

<objective>
Audit and fix social and friends screens for UI/UX consistency.

Purpose: Ensure friends, contacts, and activity screens use consistent card styling, list patterns, loading states, and interaction feedback.
Output: All social screens and components using constants consistently, matching established patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-ui-ux-consistency-audit/48-CONTEXT.md
@.planning/phases/48-ui-ux-consistency-audit/48-01-SUMMARY.md
@.planning/phases/48-ui-ux-consistency-audit/48-02-SUMMARY.md

# Design system references:

@src/constants/colors.js
@src/constants/spacing.js
@src/constants/typography.js
@src/constants/layout.js

# Screens to audit:

@src/screens/FriendsScreen.js
@src/screens/ContactsSyncScreen.js
@src/screens/ActivityScreen.js

# Style files:

@src/styles/FriendsScreen.styles.js
@src/styles/FriendCard.styles.js
@src/styles/ContactsSyncScreen.styles.js

# Components to audit:

@src/components/FriendCard.js
@src/components/AddFriendsPromptCard.js

**Audit checklist per file:**

1. No hardcoded hex/rgb/rgba color values — use colors.\*
2. No hardcoded numeric padding/margin — use spacing.\*
3. No hardcoded font families — use typography.fontFamily.\*
4. No hardcoded font sizes — use typography.size.\*
5. No hardcoded border radius — use layout.borderRadius.\*
6. No hardcoded dimensions — use layout.dimensions.\*
7. Consistent card styling, avatar sizes, action buttons
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix social screens (FriendsScreen, ContactsSyncScreen, ActivityScreen)</name>
  <files>src/screens/FriendsScreen.js, src/screens/ContactsSyncScreen.js, src/screens/ActivityScreen.js, src/styles/FriendsScreen.styles.js, src/styles/ContactsSyncScreen.styles.js</files>
  <action>
Read each screen and style file. Apply the full audit checklist:
1. Replace hardcoded colors → colors.* constants
2. Replace hardcoded spacing → spacing.* constants
3. Replace hardcoded fonts → typography.* constants
4. Replace hardcoded borderRadius → layout.borderRadius.*
5. Replace hardcoded dimensions → layout.dimensions.*

Cross-screen consistency checks:

- Tab/segment controls (if present) should use consistent active/inactive styling
- Friend list rows should use consistent height, avatar size (layout.dimensions.avatarMedium or avatarSmall), and text hierarchy
- Accept/decline/add buttons should use consistent colors and sizing
- Empty states should use consistent messaging patterns (typography, spacing)
- Loading states should be consistent (spinner/skeleton patterns)
- Screen backgrounds must be colors.background.primary

ContactsSyncScreen: Permission request UI should match established button/text patterns.
ActivityScreen: Activity items should use consistent avatar + text + timestamp patterns.
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>FriendsScreen, ContactsSyncScreen, ActivityScreen all use design system constants with consistent list patterns and interaction feedback</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix social components (FriendCard, AddFriendsPromptCard)</name>
  <files>src/components/FriendCard.js, src/components/AddFriendsPromptCard.js, src/styles/FriendCard.styles.js</files>
  <action>
Read each component and style file. Apply the full audit checklist.

Specific checks:

- FriendCard: Avatar should use layout.dimensions.avatarMedium (40) or avatarSmall (32) consistently. Name text should use typography.fontFamily.bodyBold with colors.text.primary. Username/status should use typography.fontFamily.body with colors.text.secondary. Action buttons (accept, remove, block) should use consistent sizing and colors.
- AddFriendsPromptCard: Should use colors.background.secondary for card background, consistent padding (spacing.md or spacing.lg), and standard button styling.

Ensure FriendCard's styling matches between FriendsScreen and any other screen that uses it (e.g., search results, suggestions).
</action>
<verify>npx expo lint passes, no import errors</verify>
<done>FriendCard and AddFriendsPromptCard use design system constants exclusively with consistent avatar sizes, text hierarchy, and button patterns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Standardized all social/friends screens and components to use design system constants. Ensured consistent card styling, list patterns, avatar sizes, and action buttons.</what-built>
  <how-to-verify>
    1. Run: npx expo start
    2. Navigate to Friends screen — check friend cards (avatar, name, action buttons)
    3. Check friend request cards — accept/decline buttons consistent
    4. Check suggestions section — card styling matches friend cards
    5. Navigate to Contacts Sync — check permission UI and contact list
    6. Navigate to Activity screen — check activity item styling
    7. Confirm: All screens look identical to before (no visual regressions)
    8. Check: Consistent card patterns, avatar sizes, button styles across all social screens
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual regressions or inconsistencies to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx expo lint` passes without errors
- [ ] All social screens and components use design system constants
- [ ] Friend cards consistent across all contexts
- [ ] Action buttons (accept, decline, add, remove) use consistent styling
- [ ] Visual appearance unchanged (verified by human)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No visual regressions
- 3 screens + 2 components standardized with cross-screen consistency
  </success_criteria>

<output>
After completion, create `.planning/phases/48-ui-ux-consistency-audit/48-03-SUMMARY.md`
</output>
