---
phase: 08-user-albums
plan: FIX1
type: fix
---

<objective>
Quick cosmetic fixes - simple UI text and feedback changes.

Issues: UAT-001, UAT-003, UAT-005
Scope: 3 minor/cosmetic fixes
</objective>

<context>
@.planning/phases/08-user-albums/08-ISSUES.md
@src/components/AlbumCard.js
@src/screens/AlbumPhotoPickerScreen.js
@src/components/AlbumPhotoViewer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAT-001 - Remove "New album" text from add button</name>
  <files>src/components/AlbumCard.js</files>
  <action>
In AddAlbumCard component, remove the "New album" Text element. Keep only the plus icon inside the dashed border square.
  </action>
  <verify>Visual: Add album button shows only plus icon, no text below</verify>
  <done>Add album button displays plus icon only</done>
</task>

<task type="auto">
  <name>Task 2: Fix UAT-003 - Move selected count into header</name>
  <files>src/screens/AlbumPhotoPickerScreen.js</files>
  <action>
Move "X selected" count into the header as a subtitle:
- Header title: "Select Photos"
- Subtitle below: "X selected" (smaller, gray text like #888)
- Remove the separate selection count bar

Layout:

```
    Select Photos        [Done]
    X selected
```

  </action>
  <verify>Visual: "X selected" in header, no separate bar</verify>
  <done>Selected count displays as header subtitle</done>
</task>

<task type="auto">
  <name>Task 3: Fix UAT-005 - Add toast confirmation for cover set</name>
  <files>src/components/AlbumPhotoViewer.js, src/screens/AlbumGridScreen.js</files>
  <action>
When "Set as Album Cover" completes, show a toast:
1. Create simple Toast component (or inline state-based notification)
2. Position at bottom of screen with safe area padding
3. Content: checkmark icon + "Cover set"
4. Dark background (#333), white text
5. Auto-dismiss after 2 seconds
6. Fade in/out animation
  </action>
  <verify>Set photo as cover → Toast appears "Cover set" → Auto-dismisses</verify>
  <done>Toast confirms cover set action</done>
</task>

</tasks>

<verification>
- [ ] Add button shows plus icon only
- [ ] Photo picker header shows "X selected" subtitle
- [ ] Cover set shows toast notification
- [ ] App builds without errors
</verification>

<success_criteria>

- UAT-001, UAT-003, UAT-005 resolved
- No regressions
  </success_criteria>

<output>
Create `.planning/phases/08-user-albums/08-FIX1-SUMMARY.md`
</output>
