---
phase: 26-privacy-features
plan: 01
type: execute
---

<objective>
Create settings screen, legal content screens, and navigation infrastructure for privacy features.

Purpose: Establish the UI foundation for App Store compliance - accessible settings menu with privacy policy and terms of service.
Output: SettingsScreen with menu, PrivacyPolicyScreen, TermsOfServiceScreen, navigation integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-privacy-features/26-CONTEXT.md
@.planning/phases/26-privacy-features/26-RESEARCH.md

# Prior phase context:

@.planning/phases/25-authentication-data-security/25-01-SUMMARY.md
@.planning/phases/25-authentication-data-security/25-02-SUMMARY.md

# Source files:

@src/screens/ProfileScreen.js
@src/navigation/AppNavigator.js

**Tech stack available:**

- React Navigation (native-stack, bottom-tabs)
- React Native (ScrollView, TouchableOpacity, StyleSheet)
- Ionicons for icons
- Dark theme colors exist in src/constants/

**Established patterns:**

- Screen components in src/screens/\*.js with PascalCase naming
- StyleSheet.create at bottom of file
- SafeAreaView wrapper for all screens
- useNavigation hook for programmatic navigation

**Constraining decisions:**

- Phase 26 CONTEXT: Settings uses app's existing dark theme (NOT iOS native Settings style)
- Phase 26 CONTEXT: Settings accessed via gear icon on ProfileScreen header
- Phase 26 RESEARCH: Keep legal text in constants file for maintainability
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create legal content constants and Settings/Legal screens</name>
  <files>src/constants/legalContent.js, src/screens/SettingsScreen.js, src/screens/PrivacyPolicyScreen.js, src/screens/TermsOfServiceScreen.js</files>
  <action>
    1. Create src/constants/legalContent.js with PRIVACY_POLICY_CONTENT and TERMS_OF_SERVICE_CONTENT string constants. Content should cover:
       - Privacy Policy: Data collected (photos, phone number, profile info), how used (app functionality), third parties (Firebase/Google), data retention, deletion process, contact info
       - Terms of Service: Acceptable use, content ownership, account termination, liability, dispute resolution
       Use template-style content appropriate for a photo-sharing social app. Include "Last Updated: January 2026" at top.

    2. Create SettingsScreen.js with:
       - Dark theme styling (backgroundColor: '#000000', text: '#FFFFFF')
       - Header with "Settings" title and back button
       - Menu items using TouchableOpacity rows:
         * Privacy Policy (icon: document-text-outline) -> navigates to 'PrivacyPolicy'
         * Terms of Service (icon: document-outline) -> navigates to 'TermsOfService'
         * Delete Account (icon: trash-outline, color: '#FF3B30') -> navigates to 'DeleteAccount'
       - Use Ionicons for icons
       - Each menu item: 56px height, borderBottom: 1px #333333

    3. Create PrivacyPolicyScreen.js with:
       - Dark theme styling matching SettingsScreen
       - Header with "Privacy Policy" title and back button
       - ScrollView containing PRIVACY_POLICY_CONTENT
       - Text styling: fontSize 14, lineHeight 22, color '#FFFFFF'

    4. Create TermsOfServiceScreen.js with same pattern as PrivacyPolicyScreen but for ToS content

    Use logger.debug for navigation events. Follow existing screen patterns in codebase.

  </action>
  <verify>Files exist and have valid JavaScript syntax: node --check src/constants/legalContent.js && node --check src/screens/SettingsScreen.js && node --check src/screens/PrivacyPolicyScreen.js && node --check src/screens/TermsOfServiceScreen.js</verify>
  <done>All 4 files created with correct exports, styling matches dark theme, menu items navigate correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add gear icon to ProfileScreen header</name>
  <files>src/screens/ProfileScreen.js</files>
  <action>
    Modify ProfileScreen.js header to include settings gear icon:

    1. Add import: import { useNavigation } from '@react-navigation/native';
    2. Add import: import Ionicons from '@expo/vector-icons/Ionicons';
    3. Add import: import { TouchableOpacity } from 'react-native';
    4. Get navigation with const navigation = useNavigation();
    5. Update header View to use flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center'
    6. Add TouchableOpacity with gear icon on right side:
       <TouchableOpacity onPress={() => navigation.navigate('Settings')} style={styles.settingsButton}>
         <Ionicons name="settings-outline" size={24} color="#000000" />
       </TouchableOpacity>
    7. Add settingsButton style: { padding: 8 }

    Keep existing header content (title), just add gear icon to the right.

  </action>
  <verify>npm run lint -- --quiet src/screens/ProfileScreen.js (no errors)</verify>
  <done>ProfileScreen has gear icon in header that navigates to Settings screen</done>
</task>

<task type="auto">
  <name>Task 3: Wire up Settings navigation stack</name>
  <files>src/navigation/AppNavigator.js</files>
  <action>
    Add Settings screens to navigation:

    1. Import the new screens at top of file:
       import SettingsScreen from '../screens/SettingsScreen';
       import PrivacyPolicyScreen from '../screens/PrivacyPolicyScreen';
       import TermsOfServiceScreen from '../screens/TermsOfServiceScreen';

    2. Create ProfileStackNavigator (similar to FriendsStackNavigator pattern):
       const ProfileStackNavigator = () => {
         return (
           <Stack.Navigator screenOptions={{ headerShown: false }}>
             <Stack.Screen name="ProfileMain" component={ProfileScreen} />
             <Stack.Screen name="Settings" component={SettingsScreen} />
             <Stack.Screen name="PrivacyPolicy" component={PrivacyPolicyScreen} />
             <Stack.Screen name="TermsOfService" component={TermsOfServiceScreen} />
           </Stack.Navigator>
         );
       };

    3. Update the Profile tab in MainTabNavigator to use ProfileStackNavigator instead of ProfileScreen directly:
       Change: component={ProfileScreen}
       To: component={ProfileStackNavigator}

    This allows navigating from Profile -> Settings -> Legal screens while keeping the tab bar visible.

    NOTE: DeleteAccount screen will be added in Plan 02 - don't add placeholder for it yet.

  </action>
  <verify>npm run lint -- --quiet src/navigation/AppNavigator.js (no errors)</verify>
  <done>Settings, PrivacyPolicy, TermsOfService screens accessible from Profile tab via gear icon</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes without errors
- [ ] App launches without crash
- [ ] Profile screen shows gear icon in header
- [ ] Tapping gear navigates to Settings screen (dark theme)
- [ ] Settings menu shows 3 items: Privacy Policy, Terms of Service, Delete Account
- [ ] Privacy Policy screen shows scrollable legal content
- [ ] Terms of Service screen shows scrollable legal content
- [ ] Back navigation works from all screens
- [ ] Delete Account menu item exists but shows alert "Coming soon" when tapped (Plan 02 will implement)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No lint errors
- Dark theme consistent across all new screens
- Navigation flow works: Profile -> Settings -> Legal screens -> back
  </success_criteria>

<output>
After completion, create `.planning/phases/26-privacy-features/26-01-SUMMARY.md`
</output>
