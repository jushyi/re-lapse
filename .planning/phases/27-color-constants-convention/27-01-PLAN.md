---
phase: 27-color-constants-convention
plan: 01
type: execute
---

<objective>
Document the color constants requirement in CONVENTIONS.md so Claude automatically follows color patterns when making code changes.

Purpose: Bake the Phase 16 color system into GSD workflows - Claude should always use colors.js constants, never hardcoded hex/rgb values.
Output: Updated CONVENTIONS.md with Color System section.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-color-constants-convention/27-CONTEXT.md

# Phase 16 established the color system:

@src/constants/colors.js
@src/constants/COLOR_REFERENCE.md

# Current CONVENTIONS.md to update:

@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Color System section to CONVENTIONS.md</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <action>
Add a new "## Color System" section to CONVENTIONS.md after the "React/React Native Patterns" section. The section should:

1. **Reference the source of truth** - Point to `src/constants/colors.js` as the single source of truth
2. **State the rule** - ALWAYS use color constants, NEVER hardcode hex/rgb values
3. **Document the hierarchy** - background.primary for screens, background.secondary for cards, text._, icon._, etc.
4. **Include examples** - Show correct usage for screen backgrounds, cards, text, icons
5. **Document anti-patterns** - Show what NOT to do (hardcoded hex values)
6. **Reference COLOR_REFERENCE.md** - Point to in-codebase documentation for full details

Pattern to follow:

- Match existing CONVENTIONS.md style (markdown headers, code blocks, examples)
- Keep concise but complete
- Include both "do" and "don't" examples

Key rules to document:

- Screen backgrounds must use `colors.background.primary`
- Cards/sheets use `colors.background.secondary`
- Icons use `colors.icon.*` (NOT purple)
- Purple is for interactive elements only
- No hardcoded hex values anywhere
  </action>
  <verify>
  Verify section added:

```bash
grep -A 5 "## Color System" .planning/codebase/CONVENTIONS.md
```

Verify anti-patterns documented:

```bash
grep -A 3 "Anti-pattern" .planning/codebase/CONVENTIONS.md
```

  </verify>
  <done>
- CONVENTIONS.md has new "Color System" section
- Section includes rule statement, hierarchy, examples, anti-patterns
- References colors.js and COLOR_REFERENCE.md
- Matches existing document style
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] "Color System" section exists in CONVENTIONS.md
- [ ] Section documents the "always use constants" rule
- [ ] Examples show correct import and usage patterns
- [ ] Anti-patterns clearly show what NOT to do
- [ ] References to colors.js and COLOR_REFERENCE.md included
</verification>

<success_criteria>

- CONVENTIONS.md updated with Color System section
- Documentation follows existing CONVENTIONS.md style
- Claude executing future phases will see color conventions in codebase context
- No code changes made (documentation only)
  </success_criteria>

<output>
After completion, create `.planning/phases/27-color-constants-convention/27-01-SUMMARY.md`:

# Phase 27 Plan 01: Color Constants Convention Documentation Summary

**[One-liner describing what was documented]**

## Accomplishments

- [Key outcome]

## Files Created/Modified

- `.planning/codebase/CONVENTIONS.md` - Added Color System section

## Decisions Made

[Any decisions about documentation structure, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for next phase
</output>
