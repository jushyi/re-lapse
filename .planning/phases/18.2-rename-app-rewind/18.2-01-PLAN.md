---
phase: 18.2-rename-app-rewind
plan: 01
type: execute
---

<objective>
Rebrand app from "Oly" to "Rewind" with new VHS-style app icon.

Purpose: Establish the new Rewind brand identity through app name and icon changes across all configuration.
Output: App displays as "Rewind" with retro VHS cassette icon, deep links use rewind:// scheme.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18.2-rename-app-rewind/18.2-CONTEXT.md

**Current state:**
- App name: "Oly" in app.json
- Bundle ID: com.spoodsjs.oly (iOS), com.oly.app (Android)
- Deep link scheme: lapse://, com.lapseclone.app://
- Icon: Aperture-style icon at assets/icon.png

**Target state:**
- App name: "Rewind"
- Deep link scheme: rewind://
- Icon: VHS cassette with visible tape reels (retro 80s/90s aesthetic)

**Key files:**
@app.json
@src/navigation/AppNavigator.js
@assets/icon.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VHS cassette app icon assets</name>
  <files>assets/icon.png, assets/adaptive-icon.png, assets/favicon.png</files>
  <action>
Create a retro VHS cassette tape icon:
- Classic rectangular VHS shape (not square)
- Visible tape reels (two circles) as key visual element
- 80s/90s nostalgic aesthetic with slight 3D effect
- Colors: Dark body (near-black or dark gray) with white/cream tape reels visible through windows
- Simple, recognizable silhouette that reads well at small sizes

Generate three sizes:
- icon.png: 1024x1024 (iOS app icon)
- adaptive-icon.png: 1024x1024 (Android adaptive icon foreground)
- favicon.png: 48x48 (web favicon)

Use a script similar to scripts/generate-icon.js pattern (Sharp library already in devDependencies).
The icon should be centered with appropriate padding for iOS rounded corners and Android adaptive icon safe zones.
  </action>
  <verify>All three PNG files exist in assets/ with correct dimensions: `identify assets/icon.png assets/adaptive-icon.png assets/favicon.png` or visual inspection</verify>
  <done>VHS cassette icon assets generated at correct sizes, visually match retro tape aesthetic</done>
</task>

<task type="auto">
  <name>Task 2: Update app.json with Rewind branding</name>
  <files>app.json</files>
  <action>
Update app.json expo configuration:
- Change "name": "Oly" → "name": "Rewind"
- Change "slug": "Oly" → "slug": "Rewind"
- Keep bundleIdentifier as com.spoodsjs.oly (changing requires new App Store entry)
- Keep android package as com.oly.app (changing requires new Play Store entry)
- Update camera permission text: "Allow Oly to access your camera" → "Allow Rewind to access your camera"

Do NOT change EAS projectId or owner - these are account-level settings.
  </action>
  <verify>Run `cat app.json | grep -E "name|slug|cameraPermission"` shows "Rewind" in appropriate places</verify>
  <done>App displays as "Rewind" in device home screen and app settings</done>
</task>

<task type="auto">
  <name>Task 3: Update deep link scheme to rewind://</name>
  <files>src/navigation/AppNavigator.js</files>
  <action>
Update the linking configuration in AppNavigator.js:
- Change prefixes array from ['lapse://', 'com.lapseclone.app://'] to ['rewind://']
- Remove the legacy com.lapseclone.app:// prefix (no longer needed)

Note: Deep link schemes are defined in app config and code, but actual iOS/Android URL scheme registration happens at build time via Expo plugins. The app.json doesn't need explicit scheme config because expo handles it.
  </action>
  <verify>Grep for "lapse://" in src/ returns no results: `grep -r "lapse://" src/`</verify>
  <done>Deep links use rewind:// scheme, no lapse:// references remain in source code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rewind branding with VHS icon and updated deep links</what-built>
  <how-to-verify>
    1. Run: npx expo start
    2. Open in Expo Go or simulator
    3. Verify: App icon shows VHS cassette shape (rectangular tape with visible reels)
    4. Verify: Any in-app references show "Rewind" not "Oly"
    5. Check app.json shows correct name and slug
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 02 (iris animation), or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] VHS icon assets exist at correct sizes (icon.png 1024x1024, adaptive-icon.png 1024x1024, favicon.png 48x48)
- [ ] app.json name and slug are "Rewind"
- [ ] Camera permission text references "Rewind"
- [ ] Deep link prefixes use rewind:// (no lapse:// references in src/)
- [ ] `npx expo start` launches without errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- App displays "Rewind" name and VHS cassette icon
- Deep links work with rewind:// scheme
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-rename-app-rewind/18.2-01-SUMMARY.md`:

# Phase 18.2 Plan 01: Rewind Branding & VHS Icon Summary

**[One-liner: what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list with descriptions]

## Decisions Made

- [Any decisions and rationale]

## Issues Encountered

- [Problems and resolutions, or "None"]

## Next Step

Ready for 18.2-02-PLAN.md (Camera iris animation)
</output>
